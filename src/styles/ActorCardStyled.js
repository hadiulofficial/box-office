import styled from 'styled-components';
import { DataCard } from './styled';

export const ActorCardStyled = styled(DataCard)`
.deathday {
    margin: 0;
    margin-top: 15px;
    font-weight: bold;
  }
`;